# upstream reference clone (not vendored)
plai/

# vendored SQLite amalgamation (fetched via tools/fetch_sqlite.sh)
third_party/sqlite/

# wiki database (shipped as a GitHub Release asset, copied to SD)
*.db

# build artifacts
emulator/*.o
emulator/emu
emulator/cyd_pty.path

# CYD terminal PlatformIO build output
cyd-terminal/.pio/

# device firmware (ESP-IDF) build output + generated config
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/managed_components/
firmware/dependencies.lock

# CyberHack web build artifacts (built in CI from the engine; not committed)
web/cyberhack.js
web/cyberhack.wasm
emulator/cyberhack_sim
